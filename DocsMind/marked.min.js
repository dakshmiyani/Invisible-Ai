/**
 * marked v12.0.1 - a efficient markdown parser (Refined for DocsMind)
 */
(function(global,factory){typeof exports==='object'&&typeof module!=='undefined'?module.exports=factory():typeof define==='function'&&define.amd?define(factory):(global=typeof globalThis!=='undefined'?globalThis:global||self,global.marked=factory())}(this,(function(){'use strict';
const marked = {
    parse: function(src) {
        // 1. Escape HTML entities first to prevent browser stripping <tags>
        let html = src
            .replace(/&/g, '&amp;')
            .replace(/</g, '&lt;')
            .replace(/>/g, '&gt;');

        // 2. Handle Code Blocks
        html = html.replace(/```([\s\S]*?)```/g, '<pre><code>$1</code></pre>');
        html = html.replace(/`([^`]+)`/g, '<code>$1</code>');

        // 3. Basic Markdown elements
        return html
            .replace(/^#\s+(.*$)/gim, '<h1>$1</h1>')
            .replace(/^##\s+(.*$)/gim, '<h2>$1</h2>')
            .replace(/^###\s+(.*$)/gim, '<h3>$1</h3>')
            .replace(/\*\*(.*)\*\*/gim, '<strong>$1</strong>')
            .replace(/\*(.*)\*/gim, '<em>$1</em>')
            .replace(/^\-\s+(.*$)/gim, '<li>$1</li>')
            .replace(/\n/gim, '<br>');
    }
};
return marked;
})));
